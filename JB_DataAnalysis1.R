## Working directory checked. Workspace sound. We're connected to GitHub. 

#We'll be using open source data from the Gapminder project (http://www.gapminder.org/)

install.packages("gapminder")

# Then load the package (after running the install)

library(gapminder)

# Now Load the Package

library(gapminder)

# Time to access the data frame. Hint: str stands fro structure

str(gapminder)